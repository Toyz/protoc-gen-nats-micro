{
  "name": "simple-ts-nats-example",
  "version": "1.0.0",
  "private": true,
  "description": "Simple TypeScript NATS microservice example with interceptors",
  "type": "module",
  "scripts": {
    "server": "bun run server.ts",
    "client": "bun run client.ts",
    "dev": "bun run --watch server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@nats-io/services": "^3.2.0",
    "nats": "^2.29.3"
  }
}
